# setting default apps
$TERM = kitty
$BROWSER = zen-browser --use-gl=egl --enable-features=UseOzonePlatform --ozone-platform=wayland
$EXPLORER = dolphin
$DRUNNER = fuzzel
$RUNNER = wofi --show run -i --exec-search
$LOCK = hyprlock
$BTOP = $TERM -e btop

# commands
#$SCREENSHOT = grim -g "$(slurp -d)" - | wl-copy

